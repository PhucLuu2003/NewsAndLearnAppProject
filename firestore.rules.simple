rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // CHO PHÉP TẤT CẢ - CHỈ DÙNG CHO DEVELOPMENT/TESTING
    // SAU KHI TEST XONG, THAY BẰNG RULES CHI TIẾT HƠN
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
